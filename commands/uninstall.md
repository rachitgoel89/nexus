---
name: uninstall
description: Remove the nexus status bar from Claude Code settings
allowed-tools: Bash
---

Run the nexus uninstall script to remove the status bar configuration:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/scripts/uninstall.sh"
```

Show the user the full output and confirm whether the status bar was removed or was not present.
